/* Test case that has a break statement */
let
    function trivial() =
        for i := 0 to 10
        do
            if i > 10
            then (break; 3)
            else printf(".")
in
   trivial
end